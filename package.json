{
  "name": "spellbook",
  "version": "2.0.0",
  "main": "readme.md",
  "license": "MIT",
  "scripts": {
    "test": "eslint --ext md . && markdownlint --ignore=node_modules ."
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn test"
    }
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-markdown": "^1.0.1",
    "husky": "^3.0.9",
    "markdownlint-cli": "^0.19.0"
  }
}
